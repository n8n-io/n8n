# =============================================================================
# TaxFlow Enhanced - Netlify Environment Variables Template
# =============================================================================
#
# This file documents all environment variables used in TaxFlow Enhanced
# when deploying to Netlify.
#
# INSTRUCTIONS:
# 1. Copy this file to .env.local for local development (optional)
# 2. Set these variables in Netlify dashboard for production:
#    - Go to: Site settings → Environment variables
#    - Add each variable with appropriate value
#    - Choose scope: Production, Deploy previews, or Branch deploys
#
# IMPORTANT:
# - All Vite environment variables MUST be prefixed with "VITE_"
# - These are exposed to the client-side code
# - Never put secrets or API keys here (client can see them)
#
# Version: 1.0.0
# Last Updated: 2025-11-23
# =============================================================================

# -----------------------------------------------------------------------------
# Application Settings
# -----------------------------------------------------------------------------

# Log level for application logging
# Options: "debug" | "info" | "warn" | "error"
# Production: "error" (only log errors)
# Development: "debug" (verbose logging)
# Deploy Previews: "warn" (warnings and errors)
VITE_LOG_LEVEL=error

# Use mock data instead of real calculations
# Options: "true" | "false"
# Production: "false" (use real tax calculations)
# Development: "true" (optional, for testing without data)
VITE_USE_MOCK_DATA=false

# -----------------------------------------------------------------------------
# Feature Flags
# -----------------------------------------------------------------------------

# Enable PDF export functionality
# Options: "true" | "false"
# Allows users to export tax returns as PDF documents
VITE_ENABLE_PDF_EXPORT=true

# Enable Excel export functionality
# Options: "true" | "false"
# Allows users to export tax data as Excel spreadsheets
VITE_ENABLE_EXCEL_EXPORT=true

# Enable state tax calculations (Phase 5 feature)
# Options: "true" | "false"
# Currently not implemented - set to "false"
VITE_ENABLE_STATE_TAX=false

# Enable experimental features
# Options: "true" | "false"
# Enables features still in development/testing
VITE_ENABLE_EXPERIMENTAL=false

# -----------------------------------------------------------------------------
# Analytics & Tracking (Optional)
# -----------------------------------------------------------------------------

# Enable analytics tracking
# Options: "true" | "false"
# When true, sends anonymous usage data to analytics platform
VITE_ENABLE_ANALYTICS=true

# Google Analytics 4 Measurement ID
# Format: G-XXXXXXXXXX
# Get this from: https://analytics.google.com/
# Leave empty if not using Google Analytics
VITE_GA_MEASUREMENT_ID=

# Plausible Analytics Domain
# Format: your-domain.com
# Get this from: https://plausible.io/
# Leave empty if not using Plausible
VITE_PLAUSIBLE_DOMAIN=

# -----------------------------------------------------------------------------
# Error Tracking & Monitoring (Optional)
# -----------------------------------------------------------------------------

# Sentry DSN (Data Source Name)
# Format: https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
# Get this from: https://sentry.io/
# Leave empty if not using Sentry
VITE_SENTRY_DSN=

# Sentry Environment
# Options: "production" | "staging" | "development"
# Helps organize errors by environment in Sentry dashboard
VITE_SENTRY_ENVIRONMENT=production

# Sentry Trace Sample Rate
# Options: 0.0 to 1.0 (percentage of transactions to trace)
# Production: 0.1 (10% of transactions)
# Higher values = more data but higher Sentry costs
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1

# Sentry Replay Sample Rate
# Options: 0.0 to 1.0 (percentage of sessions to replay)
# Production: 0.1 (10% of sessions)
# Session replays help debug user issues
VITE_SENTRY_REPLAYS_SAMPLE_RATE=0.1

# -----------------------------------------------------------------------------
# API Configuration (Future Use)
# -----------------------------------------------------------------------------

# API Base URL
# Format: https://api.example.com
# Currently not used (client-side only app)
# Will be needed if backend API is added in future
# VITE_API_URL=https://api.taxflow.example.com

# API Timeout (milliseconds)
# Options: any positive integer
# Default: 30000 (30 seconds)
# VITE_API_TIMEOUT=30000

# -----------------------------------------------------------------------------
# Build Configuration
# -----------------------------------------------------------------------------

# Application Version
# Format: X.Y.Z (semantic versioning)
# Automatically set by build process from package.json
# Can be manually overridden if needed
VITE_APP_VERSION=1.0.0

# Build Environment
# Options: "production" | "staging" | "development"
# Automatically set by Vite based on build command
# Usually don't need to set manually
# VITE_BUILD_ENV=production

# -----------------------------------------------------------------------------
# Performance & Optimization
# -----------------------------------------------------------------------------

# Enable service worker for offline support
# Options: "true" | "false"
# Currently not implemented - set to "false"
VITE_ENABLE_SERVICE_WORKER=false

# Enable bundle size warnings
# Options: "true" | "false"
# Shows warnings during build if bundles are too large
VITE_ENABLE_BUNDLE_WARNINGS=true

# Chunk size warning limit (KB)
# Options: any positive integer
# Default: 1000 (1MB)
# Warnings shown if chunks exceed this size
VITE_CHUNK_SIZE_WARNING_LIMIT=1000

# -----------------------------------------------------------------------------
# Development & Debugging (Development Only)
# -----------------------------------------------------------------------------

# Enable React DevTools
# Options: "true" | "false"
# Automatically disabled in production builds
# VITE_ENABLE_DEVTOOLS=false

# Enable debug mode
# Options: "true" | "false"
# Shows additional debugging information in console
# Production: "false"
# VITE_DEBUG_MODE=false

# Enable source maps
# Options: "true" | "false"
# Automatically configured by vite.config.ts
# Production: hidden source maps (for error tracking)
# VITE_ENABLE_SOURCEMAPS=false

# -----------------------------------------------------------------------------
# Netlify-Specific Variables
# -----------------------------------------------------------------------------

# These are automatically set by Netlify - DO NOT SET MANUALLY
# Documented here for reference only

# CONTEXT - Deployment context (production, deploy-preview, branch-deploy)
# URL - Full URL of the deployment (includes protocol)
# DEPLOY_URL - Full URL of the deployment (alias of URL)
# DEPLOY_PRIME_URL - Permanent URL of the deployment
# DEPLOY_ID - Unique deploy identifier
# COMMIT_REF - Git commit reference (branch, tag, or PR number)
# HEAD - Git HEAD reference (branch or PR number)
# BRANCH - Git branch name
# PULL_REQUEST - PR number (if deploy preview)
# REVIEW_ID - Unique ID for deploy preview
# REPOSITORY_URL - Git repository URL
# NETLIFY - Always "true" when building on Netlify

# =============================================================================
# DEPLOYMENT INSTRUCTIONS
# =============================================================================
#
# For Local Development:
# ----------------------
# 1. Copy this file to .env.local
# 2. Customize values as needed
# 3. File is gitignored - won't be committed
#
# For Netlify Deployment:
# -----------------------
# 1. Go to Netlify dashboard: https://app.netlify.com
# 2. Select your site
# 3. Go to: Site settings → Environment variables
# 4. Click "Add a variable"
# 5. Add each variable from this file:
#    - Key: VITE_LOG_LEVEL
#    - Value: error
#    - Scopes: Production (select as needed)
# 6. Click "Create variable"
# 7. Repeat for all required variables
# 8. Trigger a new deployment:
#    - Go to: Deploys tab
#    - Click: "Trigger deploy" → "Deploy site"
#
# Required Variables for Production:
# ----------------------------------
# ✅ VITE_LOG_LEVEL=error
# ✅ VITE_USE_MOCK_DATA=false
# ✅ VITE_ENABLE_PDF_EXPORT=true
# ✅ VITE_ENABLE_EXCEL_EXPORT=true
# ✅ VITE_ENABLE_STATE_TAX=false
#
# Optional Variables (Add as needed):
# -----------------------------------
# ⚪ VITE_ENABLE_ANALYTICS=true
# ⚪ VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX
# ⚪ VITE_PLAUSIBLE_DOMAIN=taxflow.example.com
# ⚪ VITE_SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx
#
# =============================================================================
# TROUBLESHOOTING
# =============================================================================
#
# Variables not working?
# ----------------------
# 1. Check variable name starts with "VITE_"
# 2. Verify variable is set in Netlify dashboard
# 3. Trigger a new deployment after adding variables
# 4. Clear browser cache and hard reload (Ctrl+Shift+R)
#
# How to check if variables are loaded?
# --------------------------------------
# In browser console, type:
#   import.meta.env.VITE_LOG_LEVEL
#
# Should return the value you set in Netlify.
# If returns undefined, variable is not set correctly.
#
# =============================================================================
# SECURITY NOTES
# =============================================================================
#
# ⚠️ WARNING: VITE_ variables are PUBLIC
# - All VITE_ prefixed variables are bundled into client-side JavaScript
# - Anyone can view these in browser DevTools
# - NEVER put secrets, passwords, or API keys here
# - For secrets, use Netlify Functions with environment variables
#
# ✅ Safe to use VITE_ variables for:
# - Feature flags (VITE_ENABLE_PDF_EXPORT)
# - Public API URLs (VITE_API_URL)
# - Analytics tracking IDs (VITE_GA_MEASUREMENT_ID)
# - Sentry DSN (public, rate-limited by Sentry)
# - Application version (VITE_APP_VERSION)
# - Log levels (VITE_LOG_LEVEL)
#
# ❌ NEVER use VITE_ variables for:
# - Database credentials
# - API secret keys
# - Encryption keys
# - Authentication tokens
# - Any sensitive data
#
# =============================================================================
