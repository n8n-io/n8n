# Required: Your Anthropic API key (get one at https://console.anthropic.com/)
ANTHROPIC_API_KEY=your_api_key_here

# Optional: Port for the server to listen on (default: 3000)
# PORT=3000

# Optional: Default working directory for Claude (default: current directory)
# WORKING_DIR=/path/to/your/codebase
