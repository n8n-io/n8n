{
	"name": "@n8n/performance",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"bench": "vitest bench",
		"bench:expression": "vitest bench benchmarks/expression-engine",
		"bench:baseline": "node scripts/save-baseline.mjs",
		"bench:run": "vitest bench --run --outputJson ./profiles/benchmark-results.json",
		"bench:check": "node scripts/check-regression.mjs",
		"bench:ci": "pnpm bench:run && pnpm bench:check"
	},
	"devDependencies": {
		"vitest": "catalog:",
		"n8n-workflow": "workspace:*"
	}
}
