// Theme presets live in `n8n-workflow` (`form-theme.ts`) so they can be shared
// with the backend / Instance AI. Re-exported here for the forms feature.
export type { FormTheme } from 'n8n-workflow';
export { FORM_THEMES } from 'n8n-workflow';
