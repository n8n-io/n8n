// Moved to `@n8n/stores`, next to the store it is the default state of, so module
// packages can seed a settings store without reaching into the shell.
export { defaultSettings } from '@n8n/stores/__tests__/defaultSettings';
