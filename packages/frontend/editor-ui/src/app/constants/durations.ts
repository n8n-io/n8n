/**
 * @deprecated Import from `@n8n/composables/durations` instead.
 *
 * Temporary re-export shim for the frontend modularization effort (CAT-3686).
 * These timing constants now live in `@n8n/composables`; call sites are
 * migrated off this path per-directory before it is removed.
 */
export * from '@n8n/composables/durations';
