/**
 * A feature declares its own modal keys. This fixture stands in for
 * `src/features/<feature>/<feature>.constants.ts` — the destination the shell
 * ratchet points authors at.
 */
export const EXAMPLE_FEATURE_MODAL_KEY = 'exampleFeatureModal';
