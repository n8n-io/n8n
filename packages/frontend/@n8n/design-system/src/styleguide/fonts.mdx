import { Meta } from '@storybook/addon-docs/blocks';

import FontsCharacterGrid from './components/FontsCharacterGrid.vue';

<Meta title="Style guide/Fonts" parameters={{ chromatic: { disableSnapshot: true } }} />

# Fonts

<div class="sb-unstyled">
	<FontsCharacterGrid
		previous={{ label: 'Inter (previous)', fontFamily: 'InterVariable, sans-serif' }}
		next={{
			label: 'Baumer (new)',
			fontFamily: "Baumer, 'EK Baumer Uniwidth UNLICENSED', 'EK Baumer Uniwidth', sans-serif",
		}}
	/>

    <FontsCharacterGrid
    	previous={{
    		label: 'CommitMono (previous)',
    		fontFamily: 'CommitMono, ui-monospace, monospace',
    	}}
    	next={{
    		label: 'Baumer Mono (new)',
    		fontFamily:
    			"'Baumer Mono', 'EK Baumer Mono UNLICENSED', 'EK Baumer Mono', ui-monospace, monospace",
    	}}
    />

</div>
