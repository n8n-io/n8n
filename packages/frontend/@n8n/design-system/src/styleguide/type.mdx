import { Meta } from '@storybook/addon-docs/blocks';

import PrimitivesTypeTable from './components/PrimitivesTypeTable.vue';

<Meta title="Styleguide/Type" />

# Type

We have two main typefaces. When rendering text inside the app, use `<Heading />` or `<Text />` and stick to the style and size set. Both of them share the same size and weight options.

<PrimitivesTypeTable />
