export const TOOLTIP_DELAY_MS = 500;

/** reka-ui stamps this on every popover and dialog layer it renders (N8nPopover, N8nDialog). */
export const OVERLAY_LAYER_SELECTOR = '[data-dismissable-layer]';
