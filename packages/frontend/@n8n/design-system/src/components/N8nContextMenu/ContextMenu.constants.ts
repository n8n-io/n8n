export const COLLISION_PADDING_PX = 8;

export const ITEMS_PADDING_PX = 4;

/** Shift the submenu up so its first row lines up with the trigger. */
export const SUBMENU_ALIGN_OFFSET_PX = -ITEMS_PADDING_PX;

export const SUBMENU_SIDE_OFFSET_PX = 1;
