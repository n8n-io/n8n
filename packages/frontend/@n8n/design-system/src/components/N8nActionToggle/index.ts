/**
 * @deprecated Use N8nDropdown instead. This component will be removed in a future version.
 * Migration guide:
 * - Replace `actions` prop (same format)
 * - Replace `@action` event with `@action` (same name)
 * - Replace `theme` prop (same values)
 * - Replace `iconOrientation` prop (same values)
 */
import N8nActionToggle from './ActionToggle.vue';

export default N8nActionToggle;
