// Shared with the MCP module, which opens this shell modal through the `modalOpeners`
// capability. Re-exported by `@/app/constants/modals` for the shell's own importers.
export const WORKFLOW_DESCRIPTION_MODAL_KEY = 'workflowDescription';
