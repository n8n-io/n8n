<form method='POST' action='{{actionUrl}}' class='mb-md'>
	{{#if isSecondary}}
	<button type='submit' class='app-btn app-btn--secondary'>{{label}}</button>
	{{else}}
	<button type='submit' class='app-btn'>{{label}}</button>
	{{/if}}
</form>
