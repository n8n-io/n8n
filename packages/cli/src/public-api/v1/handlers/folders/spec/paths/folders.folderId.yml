delete:
  x-eov-operation-id: deleteFolder
  x-eov-operation-handler: v1/handlers/folders/folders.handler
  tags:
    - Folders
  summary: Delete a folder
  description: Delete a folder within a project.
  parameters:
    - name: projectId
      in: path
      description: The ID of the project.
      required: true
      schema:
        type: string
    - name: folderId
      in: path
      description: The ID of the folder.
      required: true
      schema:
        type: string
    - name: transferToFolderId
      in: query
      description: Optional target folder ID to move folder contents into before deleting.
      required: false
      schema:
        type: string
  responses:
    '204':
      description: Operation successful.
    '400':
      $ref: '../../../../shared/spec/responses/badRequest.yml'
    '401':
      $ref: '../../../../shared/spec/responses/unauthorized.yml'
    '403':
      $ref: '../../../../shared/spec/responses/forbidden.yml'
    '404':
      $ref: '../../../../shared/spec/responses/notFound.yml'
