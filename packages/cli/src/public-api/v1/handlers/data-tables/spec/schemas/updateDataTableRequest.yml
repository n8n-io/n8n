type: object
properties:
  name:
    type: string
    description: New name for the data table
    minLength: 1
    maxLength: 128
  projectId:
    type: string
    description: Move this data table to a different project. Can be combined with name to transfer and rename atomically.
