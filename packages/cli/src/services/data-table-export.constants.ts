/**
 * File-format contract between the export and the import service: the export service names
 * data-table row files with this prefix, the import service routes them by it.
 */
export const DATA_TABLE_ROWS_FILE_PREFIX = 'data_table_user_';
