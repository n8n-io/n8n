import { ChatHubService } from '../chat-hub.service';

describe(ChatHubService, () => {
	describe('getModels', () => {
		it.todo('should return models from providers with valid credentials');

		it.todo('should return models from other providers even when a credential ID is invalid');

		it.todo('should return models from other providers even when an endpoint fails');
	});
});
