# Dev/testing-only override for the embedded local instance's LLM.
# When set, the embedded n8n uses Anthropic instead of local ollama.
# Copy to `.env` (gitignored) — there is deliberately no UI for this.
ANTHROPIC_API_KEY=
