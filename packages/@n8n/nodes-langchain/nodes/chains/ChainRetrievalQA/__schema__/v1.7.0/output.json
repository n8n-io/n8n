{
	"type": "object",
	"required": ["response"],
	"properties": {
		"response": {
			"type": "string",
			"description": "The answer text produced by the chain — a plain string"
		}
	},
	"version": 1
}
