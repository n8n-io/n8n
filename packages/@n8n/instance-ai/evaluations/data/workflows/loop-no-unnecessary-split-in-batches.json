{
	"description": "Anti-pattern guard: sequential per-item processing does not require Loop Over Items. Default per-item iteration already runs items one after another through the chain.",
	"conversation": [
		{
			"role": "user",
			"text": "Every 15 minutes, pull pending validation jobs from GET https://api.example.com/jobs/pending and run each through POST https://api.example.com/jobs/validate before marking it complete at PATCH https://api.example.com/jobs/{id}/complete. Each job must finish validation before the next one starts."
		}
	],
	"messageBudget": 6,
	"complexity": "medium",
	"tags": ["build", "loops", "implicit-iteration", "anti-pattern", "schedule", "http-request"],
	"triggerType": "schedule",
	"datasets": ["full"],
	"outcomeExpectations": [
		"Each pending job is validated and marked complete in sequence via default per-item iteration (fetch → validate → complete per job).",
		"The agent did NOT add a Loop Over Items / Split In Batches node — the prompt's ordering requirement is already satisfied by default item flow, not by an explicit batch loop."
	],
	"executionScenarios": [
		{
			"name": "two-jobs-processed-in-order",
			"description": "Two pending jobs each go through validate then complete",
			"dataSetup": "GET https://api.example.com/jobs/pending returns { \"jobs\": [{ \"id\": \"job-1\" }, { \"id\": \"job-2\" }] }. POST https://api.example.com/jobs/validate returns { \"ok\": true } for each job. PATCH https://api.example.com/jobs/job-1/complete and PATCH https://api.example.com/jobs/job-2/complete each return { \"status\": \"complete\" }.",
			"successCriteria": "The workflow executes without errors. Both jobs are validated (POST /jobs/validate called twice) and both are marked complete (PATCH for job-1 and job-2). No errors from miswired loop done/loop outputs."
		}
	]
}
