{
	"description": "Intent-only request: classify a scheduled weather notification as workflow-anchored with no embedded agent.",
	"conversation": [
		{
			"role": "user",
			"text": "Every weekday at 9am, Slack me the Berlin weather forecast."
		}
	],
	"complexity": "simple",
	"tags": ["intent-resolution", "scheduled", "notification"],
	"datasets": ["agents-exam"],
	"intentExpectation": {
		"accepts": [
			{
				"anchor": "wf",
				"embedsOther": false
			}
		]
	},
	"processExpectations": [
		"The agent explains that the request has a fixed schedule trigger and a fixed notification action, with no runtime reasoning or decision-making involved.",
		"The agent calls the load_skill tool with the intent-recognition skill before classifying the request."
	]
}
