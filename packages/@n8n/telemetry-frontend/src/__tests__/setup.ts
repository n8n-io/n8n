// Vitest setup file. Intentionally empty — the loader tests rely on jsdom's
// default window/document globals, which @n8n/vitest-config/frontend already
// provides via `environment: 'jsdom'`.
export {};
