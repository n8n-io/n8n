{
	"extends": "@n8n/typescript-config/tsconfig.common.go.json",
	"compilerOptions": {
		"types": ["node"],
		"module": "ESNext",
		"moduleResolution": "bundler",
		// Consumed from source, so nothing should ever emit `.js` beside these files.
		"noEmit": true
	},
	"include": ["**/*.ts"]
}
