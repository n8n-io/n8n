/** Rollout flag for a canvas group that holds its own trigger. */
export const GROUPS_WITH_TRIGGERS_FLAG = '117_flexible_groups_triggers';

/** Rollout flag for a canvas group with several entry and exit nodes. */
export const GROUPS_WITH_MANY_BOUNDARIES_FLAG = '122_flexible_groups_multiple_boundaries';
