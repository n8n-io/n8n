{
  "name": "@n8n/secret-patterns",
  "version": "0.1.0",
  "description": "Shared issuer-shaped secret regex patterns and redactors used across n8n's AI assistant and MCP servers.",
  "main": "dist/index.js",
  "module": "src/index.ts",
  "types": "dist/index.d.ts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "clean": "rimraf dist .turbo",
    "dev": "pnpm watch",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json",
    "format": "biome format --write src",
    "format:check": "biome ci src",
    "lint": "eslint . --quiet",
    "lint:fix": "eslint . --fix",
    "watch": "tsc -p tsconfig.build.json --watch",
    "test": "jest",
    "test:dev": "jest --watch"
  },
  "devDependencies": {
    "@n8n/typescript-config": "workspace:*"
  }
}
