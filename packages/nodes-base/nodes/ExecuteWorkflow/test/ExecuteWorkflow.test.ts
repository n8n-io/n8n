// Tests can not run as executeWorkflow is not available in nodes-base

// import { testWorkflows, getWorkflowFilenames } from '@test/nodes/Helpers';

// const workflows = getWorkflowFilenames(__dirname);

// describe('Test Execute Workflow Node', () => testWorkflows(workflows));
