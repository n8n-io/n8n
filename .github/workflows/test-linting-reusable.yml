name: 'Test: Linting'

on:
  workflow_call:
    inputs:
      ref:
        description: GitHub ref to lint.
        required: false
        type: string
        default: ''
      nodeVersion:
        description: Version of node to use.
        required: false
        type: string
        default: 26.5.1

env:
  # Each shard runs two workers with a 6 GB cap. This fits the runner's 15.2 GB.
  NODE_OPTIONS: --max-old-space-size=6144

jobs:
  lint:
    name: Lint (${{ matrix.name }})
    runs-on: ${{ vars.RUNNER_PROVIDER == 'github' && 'ubuntu-latest' || 'blacksmith-4vcpu-ubuntu-2204' }}
    permissions:
      contents: read
      packages: read
    container: ${{ github.event_name == 'pull_request' && github.head_ref == 'ci/pilot-toolchain-pr' && github.event.pull_request.head.repo.full_name == 'n8n-io/n8n' && inputs.nodeVersion == '26.5.1' && fromJSON(format('{{"image":"ghcr.io/n8n-io/ci-toolchain:node26.5.1-pnpm12.4.2-pw1.62.1-sc1.5.20-v1","credentials":{{"username":"{0}","password":"{1}"}}}}', github.actor, github.token)) || null }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: Backend
            command: pnpm turbo run lint --concurrency=2 --cache=local:r,remote:r --filter=!./packages/frontend/** --filter=!./packages/modules/*/frontend --filter=!./packages/extensions/**
            run-global-checks: true
          - name: Frontend
            command: pnpm turbo run lint lint:styles --concurrency=2 --cache=local:r,remote:r --filter=./packages/frontend/** --filter=./packages/modules/*/frontend --filter=./packages/extensions/**
            run-global-checks: false
    steps:
      - uses: useblacksmith/checkout@bcec731f1eb1367240608d1c889a75b96db6ec53 # v1.5.0
        with:
          ref: ${{ inputs.ref }}

      - name: Build and Test
        uses: ./.github/actions/setup-nodejs
        with:
          build-command: ${{ matrix.command }}
          node-version: ${{ inputs.nodeVersion }}
          preinstalled-toolchain: ${{ github.event_name == 'pull_request' && github.head_ref == 'ci/pilot-toolchain-pr' && github.event.pull_request.head.repo.full_name == 'n8n-io/n8n' && inputs.nodeVersion == '26.5.1' }}

      - name: Run global lint checks
        if: matrix.run-global-checks
        run: pnpm boundaries:check && pnpm check:workspace-private-deps
