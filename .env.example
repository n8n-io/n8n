# ============================================
# Development Environment Variables
# ============================================
# Copy this file to .env and customize for your setup
# Usage: docker compose -f docker-compose.dev.yml --env-file .env up -d

# GitHub Container Registry Settings
# Required for pulling pre-built dev images
GITHUB_REPOSITORY_OWNER=kodflow

# Branch name for the Docker image tag
# This should match the branch you built with scripts/build-dev-image.sh
# Example: feature-dev-enterprise-mode, dev-my-feature, etc.
BRANCH_NAME=feature-dev-enterprise-mode

# ============================================
# Alternative: Use a specific image tag
# ============================================
# Uncomment and set this to override the image entirely
# N8N_IMAGE=ghcr.io/kodflow/n8n:branch-feature-dev-enterprise-mode
