# =================
# n8n 前后端分离配置
# =================
# 本项目采用前后端完全分离架构
# 架构设计参考 Coze Studio (https://github.com/coze-dev/coze-studio)
#
# 开发环境：
#   - 主应用前端 Vite 服务器（:8080，使用 Vite proxy 代理 API 请求）
#   - 后端 API 服务器（:5678）
#   - 管理后台前端 Vite 服务器（:5679，使用 Vite proxy 代理 API 请求）
#
# 生产环境：
#   - 反向代理（Nginx/Caddy 等）+ 前端静态资源
#   - 后端 API 服务器
#   - 可选：CDN 部署前端资源
#
# 详细文档：docs/README.md

# =================
# CORS 配置（必需）
# =================
# 允许的前端源地址（多个用逗号分隔）
# 开发环境：各服务的开发端口
ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5678,http://localhost:5679
# 生产环境示例：
# ALLOWED_ORIGINS=https://app.your-domain.com,https://admin.your-domain.com

# =================
# 数据库配置
# =================
DB_TYPE=postgresdb
DB_POSTGRESDB_HOST=localhost
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8n
DB_POSTGRESDB_PASSWORD=n8n

# =================
# 后端服务配置
# =================
N8N_HOST=0.0.0.0
N8N_PORT=5678
N8N_PROTOCOL=http
NODE_ENV=development

# =================
# 可选：前端 URL 配置
# =================
# 用于后端生成邮件、回调等链接
# 开发环境
FRONTEND_URL=http://localhost:8080

# 生产环境示例：
# FRONTEND_URL=https://app.your-domain.com
