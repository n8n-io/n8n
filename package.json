{
  "name": "tls-n8n",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json"
  },
  "dependencies": {
    "@n8n/core": "workspace:*",
    "@n8n/cli": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^4.0.0",
    "tsc-alias": "^1.5.0"
  }
}
