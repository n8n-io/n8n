#!/bin/bash
# TestBot Auth Token Script for n8n
# n8n uses Basic Auth, so we output credentials in format: username:password
# Note: For Basic Auth in Skyramp tests, use these credentials

echo "testuser:testpass123"
