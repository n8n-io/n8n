Your questions have been answered: "The branch name says 'single-instance-promotion' — is the core goal to let teams use one n8n instance instead of separate dev/staging/prod instances? Understanding this shapes whether webhook URL disambiguation is a hard requirement or an edge case."="Yes — replace multi-instance deployments", "What is the expected scope of this implementation?"="Prototype — validate UX & data model", "Should data tables be in scope for this implementation? (The data-table module at packages/cli/src/modules/data-table needs to be confirmed as released/stable first.)"="Not sure — need to verify module status first", "Is this feature enterprise-only (behind a license gate), or available to all plans?"="Enterprise only". You can now continue with these answers in mind.