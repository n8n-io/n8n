# INS-701 implementation plan

1. Move the generic invisible-Unicode and untrusted-data helpers into `@n8n/agents` and re-export
   them through the existing Instance AI module.
2. Extend `BuiltTool` and `Tool` with the optional untrusted-output declaration.
3. Add runtime helpers that protect complete model results, derived messages, and error text while
   preserving media parts.
4. Apply the helper after result-size guarding in `ToolCallExecutor`, while leaving raw lifecycle
   events and tool entries unchanged.
5. Declare external MCP tools, local gateway MCP tools, and stored-result pages as untrusted.
6. Extend the Instance AI shared prompt doctrine to cover MCP result text, structured data,
   resources, media, errors, and stored pages.
7. Protect MCP connection and tool-listing failure details before adding them to the model-facing
   system status note.
8. Add focused unit and runtime tests for alternate MCP shapes, media, errors, approval wrapping,
   truncation/offloading, connection failures, and raw-event compatibility.
9. Run package tests, lint, and typecheck for `@n8n/agents` and `@n8n/instance-ai`. Run the relevant
   cross-package build with redirected output before opening the draft PR.
10. Review the final diff from the model's point of view: check every route by which tool-authored
   data can reach a tool result, assistant message, error result, or stored-result page.
