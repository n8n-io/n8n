/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 * @generated-id: a17025d29d40
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../lib/primitives.js";
import {
  collectExtraKeys as collectExtraKeys$,
  safeParse,
} from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MCPResultMetadata,
  MCPResultMetadata$inboundSchema,
} from "./mcpresultmetadata.js";

/**
 * Metadata wrapper for MCP tool call responses.
 *
 * @remarks
 *
 * Nests MCP-specific fields under `mcp_meta` to avoid collisions with other
 * metadata keys (e.g. `tool_call_result`) in Harmattan's streaming deltas.
 */
export type MCPToolCallMetadata = {
  mcpMeta?: MCPResultMetadata | null | undefined;
  additionalProperties?: { [k: string]: any } | undefined;
};

/** @internal */
export const MCPToolCallMetadata$inboundSchema: z.ZodType<
  MCPToolCallMetadata,
  z.ZodTypeDef,
  unknown
> = collectExtraKeys$(
  z.object({
    mcp_meta: z.nullable(MCPResultMetadata$inboundSchema).optional(),
  }).catchall(z.any()),
  "additionalProperties",
  true,
).transform((v) => {
  return remap$(v, {
    "mcp_meta": "mcpMeta",
  });
});

export function mcpToolCallMetadataFromJSON(
  jsonString: string,
): SafeParseResult<MCPToolCallMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MCPToolCallMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MCPToolCallMetadata' from JSON`,
  );
}
