// Generated by Skyramp v1.3.13 on 2026-03-16 12:00:48.14445419 +0000 UTC m=+148.484424492
// Command: skyramp generate integration rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output scenario-execution-workflow.test.ts \
// 		--output-dir /home/runner/work/n8n-testb/n8n-testb/skyramp-tests \
// 		--overwrite \
// 		--provider \
// 		--runtime local \
// 		--skipProvisionParents \
// 		--trace /home/runner/work/n8n-testb/n8n-testb/skyramp-tests/scenario_scenario-execution-workflow.json

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, getBaseUrl, getValue, getResponseValue } from '@skyramp/skyramp';

// URL for test requests
let URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:5678");

test('testIntegration', async () => {
    test.setTimeout(60000);

    // Invocation of Skyramp Client
    let client =  new SkyrampClient();

    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Declaration of variables
    let workflows = "wf-test-001";

    // Request Body
    const executePostRequestBody = `{
            "mode": "manual"
        }`
    
    // Execute Request
    let executePostResponse = await client.sendRequest({
        url:URL_localhost,
        path:"/scenario/executions/workflows/{workflows}/execute",
        method:"POST",
        body:executePostRequestBody,
        headers:headers,
        pathParams:{"workflows": workflows}
    });

    // Generated Assertions
    expect(executePostResponse.statusCode, 'status code').toBe(200);
    expect(getResponseValue(executePostResponse, 'data.id'), 'id').toBeDefined();

    // Execute Request
    let executionsGetResponse = await client.sendRequest({
        url:URL_localhost,
        path:"/scenario/executions/{executions}",
        method:"GET",
        headers:headers,
        pathParams:{"executions": getResponseValue(executePostResponse, "data.id")}
    });

    // Generated Assertions
    expect(executionsGetResponse.statusCode, 'status code').toBe(200);
    expect(getResponseValue(executionsGetResponse, 'data.id'), 'id').toBe(getResponseValue(executePostResponse, 'data.id'));

    // Execute Request
    let workflowsGetResponse = await client.sendRequest({
        url:URL_localhost,
        path:"/scenario/executions/workflows/{workflows}",
        method:"GET",
        headers:headers,
        pathParams:{"workflows": getResponseValue(executePostResponse, "data.workflowId")}
    });

    // Generated Assertions
    expect(workflowsGetResponse.statusCode, 'status code').toBe(200);
});

