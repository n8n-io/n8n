// Generated by Skyramp v1.3.13 on 2026-03-16 21:07:25.470240454 +0000 UTC m=+116.196820717
// Command: skyramp generate contract rest http://localhost:5678/rest/scenario/error-handling/retry-demo \
// 		--framework playwright \
// 		--language typescript \
// 		--method GET \
// 		--output error-handling-retry-demo.contract.test.ts \
// 		--output-dir /home/runner/work/n8n-testb/n8n-testb/skyramp-tests \
// 		--overwrite \
// 		--provider \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, checkStatusCode, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
let URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:5678");

// contract test for /rest/scenario/error-handling/retry-demo GET
test('testRetryDemoGet', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Execute Request
    const retryDemoGetResponse = await client.sendRequest({
        url: URL,
        path: "/rest/scenario/error-handling/retry-demo",
        method: "GET",
        headers: headers
    });

    // Generated Assertions
    expect(checkStatusCode(retryDemoGetResponse, "20x"), 'status code').toBeTruthy();
});
