// Generated by Skyramp v1.3.13 on 2026-03-16 21:07:31.467489714 +0000 UTC m=+122.194069986
// Command: skyramp generate integration rest \
// 		--framework playwright \
// 		--language typescript \
// 		--output error-handling-execute.integration.test.ts \
// 		--output-dir /home/runner/work/n8n-testb/n8n-testb/skyramp-tests \
// 		--overwrite \
// 		--provider \
// 		--runtime local \
// 		--skipProvisionParents \
// 		--trace /home/runner/work/n8n-testb/n8n-testb/skyramp-tests/scenario_error-handling-execute.json

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, getBaseUrl, getValue } from '@skyramp/skyramp';

// URL for test requests
let URL_localhost = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:5678");

test('testIntegration', async () => {
    test.setTimeout(60000);

    // Invocation of Skyramp Client
    let client =  new SkyrampClient();

    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Request Body
    const executePostRequestBody = `{
            "nodes": [
                {
                    "name": "RetryNode",
                    "settings": {
                        "continueOnFail": false,
                        "maxTries": 3,
                        "retryOnFail": true,
                        "waitBetweenTries": 1
                    },
                    "shouldFail": true
                }
            ]
        }`
    
    // Execute Request
    let executePostResponse = await client.sendRequest({
        url:URL_localhost,
        path:"/rest/scenario/error-handling/execute",
        method:"POST",
        body:executePostRequestBody,
        headers:headers
    });

    // Generated Assertions
    expect(executePostResponse.statusCode, 'status code').toBe(200);
});

