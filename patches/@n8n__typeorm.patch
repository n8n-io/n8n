diff --git a/driver/sqlite-pooled/SqliteLibrary.js b/driver/sqlite-pooled/SqliteLibrary.js
index b6bac75527e3517b5efb5fde4d73a80e1b545e3b..d2cb29b0c8c0f663e3f168278c12f3c995b075ca 100644
--- a/driver/sqlite-pooled/SqliteLibrary.js
+++ b/driver/sqlite-pooled/SqliteLibrary.js
@@ -84,6 +84,12 @@ class SqliteLibrary {
         // working properly. this also makes onDelete to work with sqlite.
         await run(`PRAGMA foreign_keys = ON`);
         await this.attachDatabases(databaseConnection);
+        // Load SQLite extensions if provided
+        if (this.options.extensions && Array.isArray(this.options.extensions)) {
+            for (const extensionLoader of this.options.extensions) {
+                extensionLoader(databaseConnection);
+            }
+        }
         return databaseConnection;
     }
     async destroyDatabaseConnection(dbConnection) {
diff --git a/driver/sqlite-pooled/SqlitePooledConnectionOptions.d.ts b/driver/sqlite-pooled/SqlitePooledConnectionOptions.d.ts
index c9ecc366a8bf884d15dec4031e02a345efc02cdb..679795f107707be77dcc6346ee0292ef693db9c8 100644
--- a/driver/sqlite-pooled/SqlitePooledConnectionOptions.d.ts
+++ b/driver/sqlite-pooled/SqlitePooledConnectionOptions.d.ts
@@ -1,4 +1,5 @@
 import { AbstractSqliteConnectionOptions } from "../sqlite-abstract/AbstractSqliteConnectionOptions";
+import type { Database as Sqlite3Database } from "sqlite3";
 /**
  * Sqlite-specific connection options.
  */
@@ -47,4 +48,10 @@ export interface SqlitePooledConnectionOptions extends AbstractSqliteConnectionO
      * How many milliseconds to wait when closing a connection.
      */
     readonly destroyTimeout: number;
+    /**
+     * Array of SQLite extension loading functions to be called on each new connection.
+     * Each function receives the database connection and should call loadExtension or similar.
+     * Example: [(db) => sqliteVec.load(db)]
+     */
+    readonly extensions?: Array<(db: Sqlite3Database) => void>;
 }
