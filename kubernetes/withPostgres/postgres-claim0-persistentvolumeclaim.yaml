apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    service: postgres-claim0
  name: postgres-claim0
  namespace: n8n
spec:
  storageClassName: "default"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
