#!/bin/sh
# Generated by Copilot using GPT-5.4.
# Repo-local Codacy CLI entrypoint for the Codacy MCP server on macOS.
# The Codacy MCP implementation only discovers .codacy/cli.sh, so this shim
# forwards all arguments to the locally installed Homebrew Codacy CLI.

exec /opt/homebrew/bin/codacy-cli "$@"